# pytest.ini
[pytest]
# ============================================================
# 測試搜尋設定
# ============================================================
testpaths = workspace/test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
# ============================================================
# 🧱 測試金字塔層級分類
# ============================================================
markers =
    # --- 測試層級（金字塔頂層） ---
    unit: 單元測試（最小功能單位，快速、無外部依賴）
    integration: 整合測試（模組之間的互動與流程驗證）
    e2e: 端對端測試（完整應用流程測試，接近使用者行為）

    # --- 測試對象（第二層分類） ---
    task: 任務模組測試（業務邏輯層，例如 load_context_task）
    tool: 工具模組測試（共用邏輯層，例如 loader / file_helper）
    controller: 控制器模組測試（流程控制層）

    # --- 具體模組或子分類（第三層標籤） ---
    loader: 測試 loader 工具
    file: 測試 file 工具
    context: 測試 load_context_task 任務

# ============================================================
# 🧩 額外設定
# ============================================================
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

addopts = -v --tb=short
